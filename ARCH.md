# 物理本质空间 — 8物理算子视觉架构

## 系统全景（零参数版 — 2026-06-24）

### 输入 → 输出

```
输入: 任意 RGB 图片 [H, W, 3]
  ↓
8 算子检测: 每像素 8 种几何基元强度 → [H, W, 8]
  ↓
场自组织: 边缘感知各向异性扩散 → 同物质向量靠拢 → [H, W, 8]
  ↓
基元发现: L2 聚类 → K 种物质
  ↓
输出: 物质掩码 + 算子签名 + 基元向量
```

**零可学习参数。零训练。零黑箱。**

---

### 1. 8 个算子 — 检测什么、输出什么

每个算子 = **固定颜色权重**（RGB→1通道投影） × **固定几何检测器**。

颜色权重来自八卦传统颜色：乾=橙红, 坤=暗绿, 震=绿蓝, 巽=全色偏亮, 坎=深蓝, 离=红, 艮=橙黄, 兑=蓝白。

| 算子 | 检测目标 | 物理含义 | 实现 | 典型效果 |
|:------|:--------|:--------|:----|:--------|
| **乾 qian** | 圆对称性 | 表面张力痕迹→液体/柔性 | 多半径环采样+方差倒数×边缘能量 | 圆物体高，角物体低 |
| **坤 kun** | 被边缘围合的内部 | 刚性边界围合→固体拓扑 | Sobel边→软边界→51×51扩散×颜色一致性 | 容器内部高，开放区域低 |
| **震 zhen** | 拉普拉斯边缘 | 折射率不连续→材料边界 | 3×3 Laplacian 核，取绝对值 | 边缘处高，平坦处低 |
| **巽 xun** | 方向纹理一致性 | 表面微观结构→粗糙度 | 4方向梯度方差倒数×梯度能量 | 光滑表面高(无方向变化)，纹理高 |
| **坎 kan** | 等照度线曲率 | 弹性变形→软硬受力差 | 梯度场归一化后求拉普拉斯 | 曲面高，平面低 |
| **离 li** | 梯度幅值(能量) | 亮度变化强度 | Sobel 梯度幅值 | 高对比区域高 |
| **艮 gen** | 局部纹理不连续 | 纹理突变→材质过渡 | 15×15块方差 + 块边界梯度 | 纹理块状区域高 |
| **兑 dui** | 中心-环绕对比 | 局部深度变化 | 5×5 vs 15×15 框差值×边缘能量 | 凹陷/孔洞处高 |

**输出**: 每算子输出 `[B, 1, H, W]` 连续值。连续值 = 强弱信息（H0：物质身份 = 强度区间模式，不是二值有/无）。

**效果**（Caltech101 113 张统计）：kun 在 85% 图片的 M1（最大物质）前三算子中。封闭物体 kun 均值 0.6~0.8，纹理表面 gen 均值 0.5~0.6，曲面 kan 均值 0.35~0.45。

---

### 2. 融合 — 8 维本质向量

当前零参数版不做可学习融合。直接用 **8 个算子响应堆叠为 8 维向量**，每像素一个。

```
算子响应 [B, 1, H, W] × 8
  → 稳健归一化（百分位，抗离群）
  → 空间预平滑（高斯核，σ=3，消除像素噪声）
  → 堆叠 → [B, 8, H, W]
```

每像素 (h,w) 的 8 维向量 = `[qian, kun, zhen, xun, kan, li, gen, dui]` — 该位置的几何基元签名。

**为什么不是 64 维**：旧版用可学习 A 核做外积（8×8→64），但 A 核需要训练 = 黑箱。零参数版直接用 8 维算子向量。H2（二阶交互充分性）待 64 维外积实验验证。

---

### 3. 本质场 — 怎么实现的

8 维向量场经过**边缘感知各向异性扩散**变为本质场。

```
初始场 Φ₀ = [qian, kun, ..., dui] per pixel (堆叠后)

迭代 (t = 0, 1, ..., N):
  Φ_{t+1}[p] = Φ_t[p] + α × Σ_{q∈4邻域} g(edge) × (Φ_t[q] - Φ_t[p])
  其中 g(edge) = exp(-(震+离) × 5)  ← 边缘处趋近0，阻断扩散
  步长 α = 0.15, 迭代 20~30 轮
```

**物理直觉**：场在同物质内部自由扩散（向量趋于一致），在边缘处被阻断（边界两侧保持差异）。类似热传导在各向异性介质中——物质边界 = 热绝缘层。

**效果**：扩散后分离度提升 +13%。收敛 δ < 0.01。

代码: `src/field_core.py` → `edge_aware_diffusion()`

---

### 4. 物质信息显示

扩散后的场 → L2 归一化 → K-means 聚类 → 每簇 = 一种物质。

输出:
- **物质掩码** `[H, W] bool` — 每像素属于哪种物质
- **基元向量** `[8]` — 该物质在 8 算子空间的原型坐标
- **算子签名** — 前 3 个主导算子 + 强度值

示例 (test_maccup.png):
```
M1 (杯体): 50.0%  kun=0.848, kan=0.228, xun=0.140
M2 (桌面): 28.5%  gen=0.557, kan=0.321, kun=0.249
M3 (过渡): 21.5%  kun=0.469, kan=0.351, xun=0.159
```

物理解释：杯体 = 高容器 + 中曲率 + 低纹理 → 陶瓷杯的几何签名。桌面 = 高块状纹理 → 背景布的几何签名。

代码: `src/primitive.py` → `discover_primitives()`, `src/material.py` → `MaterialReader`

---

## 核心数据结构

旧八卦语义算子（乾=圆、坤=平...）是人为解释，不是物理测量。新架构：8 算子测量可验证的物理量，理解从物理推演产生。

- 每个像素携带 64 维向量 = 该位置"物质本质"的描述
- 同一物质的像素 → 64 维向量相似
- 不同物质的像素 → 64 维向量不同
- 64 维向量不是图片级别的描述，是像素级别的描述

**不要取全图均值。取均值 = 丢弃空间信息 = 背离架构核心。**

## 核心架构（零参数版 — 2026-06-24 突破）

```
图像 → 8个固定算子(颜色投影+几何检测) → 稳健归一化 → 空间预平滑
     → 堆叠为8维本质向量场 → 边缘感知各向异性扩散 → L2聚类 → 物质自发呈现
```

**零可学习参数。零训练。零黑箱。**

物理直觉：
- 8 算子 = 8 种几何基元的物理测量（圆度、容器、边缘、纹理、曲率、能量、块状、对比）
- 边缘感知扩散 = 场自组织——同物质内部向量靠拢，边缘处被阻断
- 聚类 = 离散物质自然浮现

## 算子版本演变（历史）

| 版本 | 文件 | 颜色处理 | 训练后分离度 | 通过率 | 物体完整度 |
|:----|:----|:--------|:----------|:-----|:---------|
| 灰度版 (gray) | `operators_gray.py` | x.mean(dim=1) 丢弃颜色 | ~10 | <50% | ❌ 大量空洞 |
| RGB版 (rgb) | `operators_rgb.py` | 三通道分别跑算子取max | ~2.3 | ~58% | ❌ 空洞更多 |
| 颜色调制版 (colormod) | `operators_colormod.py` | 可学习1×1颜色卷积(3→1) | ~3.1 | 58% | ⚠️ 80%空洞解决 |
| 颜色原生版 (native) | `operators_native.py` | RGB全通道梯度，无选择性 | ~4.2 | 58% | ❌ 噪声叠加 |
| **固定颜色+原始强度** (fixedcolor) | **`operators_fixedcolor.py`** | **固定颜色投影+原始强度+instancenorm** | **~15.6** | **100%** | **✅ 全部完整** |

当前算子：**固定颜色版 (fixedcolor)**。每个算子用固定的八卦颜色权重（RGB三通道投影），不参与训练。

## 8 算子（固定数学运算）

| 卦 | 算子 | 检测目标 | 物理含义 |
|:--|:----|:--------|:--------|
| 乾 | 圆对称性 | 圆形/全局结构 | 表面张力痕迹 → 液体/柔性材料 |
| 坤 | 被边缘围合的内部 | 容器/封闭区域 | 刚性边界围合 → 固体拓扑 |
| 震 | 拉普拉斯 | 边缘/突变 | 折射率不连续 → 材料边界 |
| 巽 | 方向梯度一致性 | 细长纹理/方向性蔓延 | 表面微观结构 → 粗糙度 |
| 坎 | 等照度线曲率 | 弯曲/流动 | 弹性变形 → 软硬材料受力差异 |
| 离 | 梯度幅值 | 视觉能量 | 亮度变化强度 |
| 艮 | 局部纹理不连续性 | 块状/遮挡边界 | 纹理突变 → 材质过渡 |
| 兑 | 中心-周围对比度 | 凹陷/缺口 | 局部深度变化 |

每个算子输出 [B, 1, H, W]。

## 当前状态

> **2026-06-24 重大转折：零参数管线验证通过。不需要可学习参数。**

### ✅ 已验证

- ✅ 8 个固定几何算子（固定颜色权重，零训练参数）
- ✅ 零参数管线：算子 → 稳健归一化 → 空间预平滑 → 边缘感知扩散 → L2聚类 → 物质自发呈现
- ✅ 单图物质发现：test_maccup.png 杯体填充 81.1%
- ✅ Caltech101 泛化性：113/113 张图全部收敛，不同物质有明确的不同算子签名
- ✅ 物理可解释性：kun(容器) 主导封闭物体（直升机0.81），gen(块状) 主导纹理表面（豹脸0.63）
- ✅ 场自组织扩散提升分离度 +13%
- ✅ 5 个算子版本 (gray/rgb/colormod/native/fixedcolor) — 历史参考
- ✅ 声纳可视化面板（`src/visualize.py`）

### ❌ 待解决

- ❌ 同色扁平物体未分离（圆+矩形合并为一个物质）→ 颜色权重需要更精细
- ❌ 跨图片物质类型匹配（Hungarian 已实现，未系统测试）
- ❌ 多物体场景自动分割（当前固定 K 聚类，不自适应）
- ❌ 64维外积场未验证（零参数版用 8 维场，64 维需要 N 算子外积）
- ❌ 基本单元压缩 / 算子扩展（框架支持，未做实验）

### ⚪ 不再需要

- ⚪ 可学习投影层（1×1 conv）→ 被空间预平滑替代
- ⚪ 可学习 A 核（双线性融合）→ 零参数模式不需要
- ⚪ InfoNCE 自举训练 → 架构不再依赖任何训练
- ⚪ 坤容器 x10 放大修复 → 零参数管线自然工作

### 当前文件结构

```
src/                          ← 核心代码（6 个文件）
  operators.py                ← 当前算子（fixedcolor = 固定颜色权重）
  field_core.py               ← **零参数本质场核心管线**
  diffusion.py                ← 场自组织扩散模块（FieldDiffusion + Bilateral）
  primitive.py                ← 基元发现（K-means / DPC + Hungarian 跨图匹配）
  material.py                 ← 物质读出头
  visualize.py                ← 声纳面板 + argmax 复合 + blended 混合
  pipeline.py                 ← 旧流水线（legacy，保留供参考）
scripts/                      ← 测试脚本（6 个）
  test_material.py            ← MVE 单图验证
  test_generalize.py          ← 批量泛化性测试（--dir 目录扫描）
  test_cross_match.py         ← 跨图物质匹配实验
  dump_prototypes.py          ← 原型向量数据导出
  sample_caltech.py           ← Caltech101 采样
  download_caltech.py         ← Caltech101 下载
legacy/                       ← 历史版本
  operator_versions/          ← 5 版算子（gray/rgb/colormod/native/fixedcolor）
  old_models/                 ← 旧分类模型
archive/                      ← 不再使用的旧脚本
  old_src/                    ← 远古实验代码
  phase1-3/                   ← 阶段实验
  diagnostic_scripts/         ← 诊断脚本
data/
  caltech101/101_ObjectCategories/  ← Caltech101 完整数据集（102 类）
  mini/                             ← 8 张马克杯变体
```

## 理论核心

4 个可证伪假设（详见 `docs/theory.md` 或计划文件）：

| 假设 | 陈述 |
|------|------|
| H0 连续值 | 算子强度梯度（强/弱/无）是物质身份的核心编码 |
| H1 基元完备性 | N 个几何基元算子的连续强度组合可完全描述任意物质 |
| H2 交互充分性 | 二阶共现（外积）信息量 ≥ 物质定义所需 |
| H3 场自组织 | 扩散 + 凝聚后同物质像素自发汇聚为同一基元 |

## 训练（旧版 — 不再需要，仅保留参考）

- 所有算子输出非负
- 投影权重 clamp ≥ 0
- 下层不依赖上层（动不读刚）

## 设计原则

可训练参数（固定颜色版）：
- A核：64 参数（双线性融合）
- 投影层：8×(1×1 conv) = 72 参数
- 颜色卷积层：0 参数（颜色权重固定为 register_buffer）
- 总计：~136 参数
