# Essence Field — 架构设计

> v1.0 — 2026-06-26

## 系统全景

```
输入: 任意 RGB 图片
  ↓
表象层: 物质如何回应光（6维）
  · 亮度对比 / 色相对比 / 纹理粗细 / 边缘密度 / 方向一致 / 高光
  ↓
抽象层: 物质的内在结构（8个表达维度）
  · 圆度 围合 边缘 纹理方向 曲率 能量 块状 凹陷
  ↓
本质层: 表象⊗抽象 → 14维联合场
  · 联合边缘度规（8算子+表象梯度max）
  · 边缘感知各向异性扩散
  · 流域分割（自动发现物质数，无需指定K）
  ↓
输出: 物质掩码 + 表达签名
```

**零学习参数。零训练。材料从场中自发涌现。**

---

## 表达维度框架

表达维度 WHAT → 三层架构 HOW 的完整映射：

| 表达维度 | 测什么 | 架构层 | 当前状态 |
|----------|--------|--------|----------|
| **光交互** || **表象层** | |
| 反射类型 | 镜面/漫反射/ subsurface | 高光强度 + 局部对比度 | ✅ |
| 透明度 | 全透/半透/不透 | (待补) | ❌ |
| 自发光 | 无/弱/强 | (待补) | ❌ |
| **本体** || **抽象层** | |
| 形态-圆度 | 圆形/对称 | 多半径环采样×方差 | ✅ |
| 形态-延伸度 | 细长/紧凑 | (待补) | ❌ |
| 表面-纹理方向 | 各向异性纹理 | 4方向梯度方差 | ✅ |
| 表面-纹理密度 | 纹理粗糙度 | 15×15块方差 | ✅ |
| 表面-纹理周期性 | 重复模式 | (待补) | ❌ |
| 体量-围合 | 被边界包围 | Sobel→软边界→扩散 | ✅ |
| 体量-凹陷 | 局部孔洞 | 中心环绕对比 | ✅ |
| **关系** || **本质层** | |
| 遮挡 | 前后关系 | (待实现) | ❌ |
| 接触类型 | 硬/软边界 | 硬边界(当前) | ⚠️ |
| 尺度 | 相对大小 | (待实现) | ❌ |

---

## 8 个表达维度

每个维度 = 固定颜色权重(RGB→1ch) × 固定几何计算。

| 维度 | 检测 | 物理对应 |
|------|------|----------|
| 圆度(qian) | 多半径环采样+方差倒数 | 液体/柔性材料 |
| 围合(kun) | Sobel→软边界→51×51扩散 | 容器/封闭体 |
| 边缘(zhen) | 3×3 Laplacian | 材料边界 |
| 纹理方向(xun) | 4方向梯度一致性 | 粗糙度/方向性 |
| 曲率(kan) | 梯度归一化→曲率 | 弯曲/流动 |
| 能量(li) | Sobel 梯度幅值 | 亮度变化强度 |
| 块状(gen) | 15×15块方差+块边界 | 纹理突变 |
| 凹陷(dui) | 5×5-15×15框差×边缘 | 孔洞/缺口 |

---

## 本质层实现

### 联合边缘度规

```
edge = max(grad(qian), grad(kun), ..., grad(dui),
           grad(L_contrast), grad(chroma_contrast), ...)
```

8 个抽象维度 + 表象维度的梯度最大值 → 捕获所有边界类型。

### 边缘感知扩散

```
Φ_{t+1} = Φ_t + α × Σ g(edge) × (Φ_neighbor - Φ_t)
g(edge) = exp(-edge × scale)
```

同物质内部向量趋于一致，边缘处阻断。

### 流域分割（替换 K-means）

```
场梯度 + 边缘 → 高程图 → 高斯平滑 → 局部极小值 = 物质中心
→ Watershed → 合并小盆地 → 物质掩码
```

零外部参数。物质数自动发现。

---

## 已验证结论

| 结论 | 证据 |
|------|------|
| 表达维度 + 扩散 → 物质自发呈现 | Caltech101 113/113 |
| 围合(kun) 光照不变 | CV=2%（3x亮度变化） |
| 键帽完整性 | v1.0 Watershed 解决（豆包确认） |
| 跨图匹配几何一致类 | 海豚 0.98, 飞机 0.99 |
| 物质 ≠ K-means 聚类 | 已移除 K-means |

## 已知局限

- 透明体无法识别（缺透明度表达维度）
- 同色异质无法区分（陶瓷vs塑料）
- 关系层未实现（遮挡/接触类型/尺度）
- 表象⊗抽象外积未实现（48维融合场）

---

## 文件结构

```
src/
  operators.py          ← 8 个固定表达维度
  field_core.py         ← 三层本质场管线
  field_dynamics.py     ← 场动力学（引力/斥力/摩擦）
  diffusion.py          ← 场自组织扩散
  primitive.py          ← 基元发现 + 跨图匹配
  material.py           ← 物质读出
scripts/
  run.py                ← 一键物质发现
  compare.py            ← 二图物质对比
  test_generalize.py    ← 批量泛化测试
  report.py             ← 理解报告
  test_cross_match.py   ← 跨图匹配批量
legacy/                 ← 历史版本
archive/                ← 废弃脚本
data/                   ← 数据集
```

## 分支

| 分支 | 内容 | 状态 |
|------|------|------|
| `main` (v0.1) | 零参数基线 | ✅ 已验证 |
| `feat/global-context` (v1.0) | 三层链路+Watershed | 🧪 当前开发 |
| `feat/proposal-mode` | 提案模式(v2.0储备) | 🔬 研究 |
| `feat/physical-ops` | 物理算子因果链 | 📦 参考 |
